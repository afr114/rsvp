<header class="nav-bar">
  <%= link_to 'Rsvp', root_path, class: "logo" %>
    <ul class="landing-tabs">
  <% if user_signed_in? %>
      <li><%=link_to "My Events", user_path(current_user)%></li>
      <li><%=link_to "Log Out", destroy_user_session_path, method: :delete %></li>
      <p>signed in as <%=current_user.first_name%><p>
  <% else %>
      <li><%= link_to "About", about_path %></li>
      <button class="btn btn-custom"><%= link_to "Sign in", new_user_session_path %> </button>
    </ul>
  <% end %>
</header>
