<h1>Sign up for RSVP</h1>

<p>Hello <%= @name %>!</p>

<p> <%= @event.user.first_name %> has invited you to <%= @event.title %>! Check out the event details at RSVP. See the guest list and plan your travels around your friends plans.

</p>

<% if Rails.env.development? %>
  <p><%= link_to "Sign up for RSVP", new_user_registration_url(:event => @event.id, :host => "localhost:3000" ) %> and start planning!</p>

<% else %>
  <p><%= link_to "Sign up for RSVP", new_user_registration_url(:event => @event.id, :host => "rsvp-list.herokuapp.com"  ) %> and start planning!</p>
<% end %>
